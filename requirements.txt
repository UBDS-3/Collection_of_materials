# Compatibility export for pip-based environments and GitHub Actions.
# pyproject.toml is the primary dependency definition for local `uv sync`.
numpy>=1.26,<2
matplotlib>=3.8,<4
jupyter>=1.0,<2
ipykernel>=6.29,<8
nbclient>=0.10,<1
pandas>=2.1,<3
